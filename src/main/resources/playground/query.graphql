query MemberQuery {
    memberQuery {
        getMemberByPhoneNumber(input: {
            phoneNumber: "212121212"
        }) {
            id
            phoneNumber
            name
            userImage
        }
    }
}
